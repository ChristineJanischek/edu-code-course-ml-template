{
  "name": "ML-Unterrichtsprojekt",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "waitFor": "postCreateCommand",
  "postCreateCommand": "python --version && git --version && python -m venv .venv && test -x .venv/bin/python && .venv/bin/python -m pip install -r requirements.txt && .venv/bin/python -m ipykernel install --sys-prefix --name ml-template --display-name 'Python (.venv)' && .venv/bin/python tests/vorabcheck.py",
  "forwardPorts": [8888],
  "portsAttributes": {
    "8888": {
      "label": "JupyterLab",
      "onAutoForward": "notify"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
      }
    }
  }
}